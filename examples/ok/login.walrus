fn main()  {
    let secret = 42;
    let max_length = 20;
    let password = "password";

    print("Password: ");
    let line = read_line();

    if line == password {
    	print("The secret is : " + int_to_string(secret) + "\n");
    } else {
        print_error("Incorrect password");
    }
}
